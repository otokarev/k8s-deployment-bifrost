- name: Deploy all
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    additional_zones:

  tasks:
  - name: Deploy Kubernates cluster
    include_tasks: deploy/cluster.yml
  - name: Deploy Stellar
    include_tasks: deploy/stellar.yml
